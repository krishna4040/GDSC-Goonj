import React from 'react'

const Gap = ({ height }) => {
    return (
        <div className={`w-full bg-darkgray`} style={{
            height: `${height}rem`
        }}></div>
    )
}

export default Gap